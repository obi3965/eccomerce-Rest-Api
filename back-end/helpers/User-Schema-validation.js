// const Joi = require('@hapi/joi')

// const authSchema = Joi.object({
//     name: Joi.string().alphanum().min(3).max(30).required().label('the name is too short'),
//     email: Joi.string().email().required().label('not a valid email'),
//     password: Joi.string().min(5).required().label('password is too short'),
//     confirmPassword: Joi.ref('password'),
//   })
  
//   module.exports ={
//       authSchema
//   }